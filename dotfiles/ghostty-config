# ── hermes-host-bootstrap Ghostty config ──
# Lives at ~/.config/ghostty/config — Ghostty reloads on save (Cmd+Shift+,).
# Docs: https://ghostty.org/docs/config

# ── Appearance ──────────────────────────────────────────────────────
# Theme is omitted on purpose — Ghostty 1.3.x reorganized its built-in
# theme bundle and `catppuccin-mocha` (an old built-in) isn't always
# resolvable, which raises a "Configuration Errors" dialog on launch.
# Pick a theme yourself once you know what's installed:
#   ghostty +list-themes 2>&1 | less
# then set e.g.:
#   theme = "Tokyo Night"
# Ghostty's default (no theme directive) is perfectly readable.
#
# theme = "Tokyo Night"
font-family = "JetBrainsMono Nerd Font"
font-size = 14
font-feature = -calt          # disable ligatures (subjective; flip to +calt if you like them)

# Padding & cursor
window-padding-x = 12
window-padding-y = 12
cursor-style = bar
cursor-style-blink = false

# Tab bar
window-theme = ghostty
macos-titlebar-style = tabs

# ── Behaviour ───────────────────────────────────────────────────────
copy-on-select = clipboard
mouse-hide-while-typing = true
confirm-close-surface = false
window-save-state = always
scrollback-limit = 100000

# Open new tab/window in same directory as current
window-inherit-working-directory = true

# Quit only when last surface closes (instead of Cmd+Q killing instantly)
quit-after-last-window-closed = true

# ── Compatibility ───────────────────────────────────────────────────
# Advertise TERM=xterm-256color instead of the default xterm-ghostty.
# Every Unix server already has xterm-256color terminfo; xterm-ghostty
# does not, which makes tmux refuse to start on remotes that haven't
# manually installed Ghostty's terminfo entry. The trade-off is that
# we lose Ghostty's most advanced terminal capabilities (undercurl
# colors, extended color signaling — features tmux mostly can't pass
# through anyway). We keep 24-bit truecolor, 256-color palette, mouse
# handling, and every TUI keeps working.
#
# If you want full xterm-ghostty fidelity, comment this line out and
# install Ghostty's terminfo on each remote:
#   /opt/homebrew/opt/ncurses/bin/infocmp -x xterm-ghostty | ssh HOST -- tic -x -
# (requires Homebrew ncurses since Apple's infocmp is too old).
# See https://ghostty.org/docs/help/terminfo
term = xterm-256color

# ── macOS keybindings ───────────────────────────────────────────────
# (Ghostty applies these globally; on Linux you'd swap super → ctrl+shift)
keybind = super+t=new_tab
keybind = super+w=close_surface
keybind = super+shift+w=close_window
keybind = super+n=new_window
keybind = super+d=new_split:right
keybind = super+shift+d=new_split:down
keybind = super+left_bracket=previous_tab
keybind = super+right_bracket=next_tab
keybind = super+k=clear_screen
